# VLARLKit OpenPI Backend

OpenPI model backend for VLARLKit. This package keeps OpenPI-specific
dependencies and model code outside the core `vlarlkit` package while still
running training in the main `torchrun` process.
